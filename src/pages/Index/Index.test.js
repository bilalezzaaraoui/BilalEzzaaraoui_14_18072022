import { render, screen } from "@testing-library/react";
import Index from "./Index";

describe("Index page", () => {
  test("It should rend a form", () => {
    // render(<Index />);
    // const formElement = screen.getByRole("form");
    // expect(formElement).toBeInTheDocument();
  });
});
